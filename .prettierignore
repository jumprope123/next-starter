# 생성물 / 잠금 파일 — prettier 가 건드리면 도구가 만든 포맷과 계속 충돌한다.
.next/
out/
build/
node_modules/
pnpm-lock.yaml
next-env.d.ts

# 패키지 매니저가 직접 쓰는 파일 (pnpm 이 자체 포맷으로 다시 씀)
package.json

# 로컬에 설치된 에이전트 스킬 (git 추적 대상 아님) — 외부 소스 그대로 유지한다.
# 재포맷하면 skills-lock.json 의 computedHash 와 어긋나 `npx skills` 의 드리프트 검사가 깨진다.
.agents/
.claude/skills/
